# Plan to upgrade our CLI. 
 
- When using launch command for a model, don't ask to configure, only the first time. If it have a configuration, launch it directly. 
- See the VRAM + RAM + CPU comsuption from the computer and the model on start screen and on monitoring screen, use htop or something similar